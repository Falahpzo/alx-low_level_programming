#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints the character 'A' followed by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	putchar(65);
	putchar('\n');
	return (0);
}
