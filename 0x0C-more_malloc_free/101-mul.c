#include "main.h"
#include <stdlib>
#include <stdio.h>

int find_len(char *str);
char *create_parray(int size);
char *iterate_zeroes(char *str);
void get_prod(char *prd, char *mult, int digit, int zeroes);
void add_nums(char *final_prod, char n*next, int next_length
